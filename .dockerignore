# The image installs only the pinned benchmark environment. Repository source
# is bind-mounted at runtime, so no benchmark data or derived artifact belongs
# in the Docker build context.
**
!.dockerignore
!docker/
!docker/Dockerfile
!requirements/
!requirements/bench.txt
